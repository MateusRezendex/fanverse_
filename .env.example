# Variáveis usadas pelo docker-compose.yml.
# Copie para .env (na raiz) e ajuste se quiser sair do default.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sabor
POSTGRES_PORT=5432

BACKEND_PORT=3000
CORS_ORIGIN=*
